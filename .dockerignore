# Git
.git
.gitignore

# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/
env/

# Environment
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Docs / config (not needed at runtime)
README.md
nixpacks.toml
netlify.toml
Procfile

# Frontend and tooling (excluded from the backend Docker image)
TariffUI/
netlify/
scripts/
tests/

# Uncompressed dataset (the .gz copy is committed and used at runtime)
backend/data/tariffs.json
backend/data/embeddings.npz
backend/data/embeddings.json
backend/data/embeddings_meta.json
