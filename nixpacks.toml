[phases.setup]
nixPkgs = ["python310", "python310Packages.pip", "python310Packages.gunicorn"]

[phases.install]
commands = [
  "python -m pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[phases.start]
command = "gunicorn --pythonpath . app:app"