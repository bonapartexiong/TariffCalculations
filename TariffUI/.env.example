# Frontend environment (copy to .env.local for local development)
#
# Leave empty to call the same-origin /v1/* routes (used by the Netlify
# Functions deployment). Set it to a full URL when the Flask backend runs
# separately (e.g. http://localhost:5000 or https://your-api.onrender.com).
NEXT_PUBLIC_API_URL=
